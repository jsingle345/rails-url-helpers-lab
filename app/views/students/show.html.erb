<h1><%= @student.to_s %></h1>


<p>This student is currently <%= @student.active ? "active" : "inactive"%>.</p>

<%= link_to("Change Activation", activate_student_path(@student)) %>

<%= link_to("Students", students_path(@students)) %>


